version: "3.8"
services:
  aluguel:
    stdin_open: true
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - DISPLAY=172.28.112.1:0.0
    command: npm start
    volumes:
      - "/app/node_modules"
      - "./:/app"